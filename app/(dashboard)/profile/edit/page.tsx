import EditProfilePageClient from '@/components/dashboard/profile/EditProfilePageClient';

export const metadata = {
  title: 'Edit Profile — DriveSafely',
  description: 'Update your DriveSafely profile details and password.',
};

export default function EditProfilePage() {
  return <EditProfilePageClient />;
}
