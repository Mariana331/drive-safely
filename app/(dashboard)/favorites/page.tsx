import FavoritesPageClient from '@/components/dashboard/favorites/FavoritesPageClient';

export const metadata = {
  title: 'Favorites — DriveSafely',
  description:
    'Saved news, practice tests, video analyses, and AI assistant answers in one place.',
};

export default function FavoritesPage() {
  return <FavoritesPageClient />;
}
